{
    "builds": [{
        "src": "spots_detector/wsgi.py",
        "use": "@now/python",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "spots_detector/wsgi.py"
        }
    ]
}